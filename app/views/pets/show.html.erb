<h1><%= @pet.name %></h1>
<p>Address: <%= @pet.address %></p>
<p>Species: <%= @pet.species %></p>
<p>Found on: <%= @pet.found_on %></p>

<%= link_to 'Back to all pets', pets_path %> |
<%= link_to 'Edit pet', edit_pet_path %> |
<%= link_to 'Remove pet', pet_path(@pet), data: { turbo_method: :delete, turbo_confirm: "Do you realle want to remove #{@pet.name}?" } %>
